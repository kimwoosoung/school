-프로그래밍
아두이누란?
) setup()loop()
-제일 먼저 실행되는 함수.
-setup()함수 종료 후 loop() 함수 실행
) loop()
-loop() 함수 내의 코드는 무한 반복됨

 Blink 예제
) PinMode()
-핀의 입출력 결정
-첫번째 인자 -> 핀 번호
-두번째 인자 -> 입출력 결정
) digitalWrite()
-디지털 값 출력
-첫번째 인자 -> 핀 번호
-두번째 인자 -> 핀 상태
) delay()
-괄호 안의 시간 만큼 지연
-단위는 ms

